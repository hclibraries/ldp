{
    "displayName": "Missing Items",
    "description": "Report of missing and lost items within a date range.",
    "instructions": "Choose a start and end date for filtering by status date. If no dates are provided, the report will default to the last 5 years.",
    "parameters": [
        {
            "name": "start_date",
            "displayName": "Start Date",
            "type": "date",
            "required": false
        },
        {
            "name": "end_date",
            "displayName": "End Date",
            "type": "date",
            "required": false
        }
    ]
}
